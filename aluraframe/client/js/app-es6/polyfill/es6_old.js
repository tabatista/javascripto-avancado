/*
Quando adicionamos metodos no prototype de uma classe ou funcao construtora,
todas as instancias dessa funcao construtora ou classe terao o metodo.
*/

//nao precisa mais pela existencia do babel
/*
if (!Array.prototype.includes) {

    // Se não existir, adiciona

    console.log('Polyfill para Array.includes aplicado.');

    Array.prototype.includes = function (elemento) {
        return this.indexOf(elemento) != -1;
    };
}
*/

